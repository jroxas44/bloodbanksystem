{
  "name": "bloodbank-nuxt",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "setup:local": "npm install && cp .env.example .env && npx prisma migrate dev --name init && npx tsx prisma/seed.ts",
    "setup:docker-mysql": "npm install && docker run -d --name bloodbank-mysql -e MYSQL_ROOT_PASSWORD=bloodbank123 -e MYSQL_DATABASE=bloodbank -p 3306:3306 mysql:8.0 && cp .env.example .env && npx prisma migrate dev --name init && npx tsx prisma/seed.ts",
    "db:seed": "npx tsx prisma/seed.ts",
    "db:migrate": "npx prisma migrate dev",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "lucide-vue-next": "^1.0.0",
    "mariadb": "^3.5.2",
    "nuxt": "^4.4.4",
    "prisma": "^7.8.0",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "mysql2": "^3.22.3"
  }
}
